[project]
name = "ultimate-repair-center"
version = "0.2.0"
description = "URC: restore-only multi-agent repair orchestration"
readme = "README.md"
requires-python = ">=3.10"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
